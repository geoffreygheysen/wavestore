{# <pre>
    {{ dump(product.images[0]) }}
</pre> #}
<div class="col-6 mb-3">
    <div class="card border-0">

            <div class="col my-1">
            <img src="{{ asset('images/products/' ~ product.images[0]) }}" alt="" class="card-img-top rounded-0">
            </div>

        {# <img src="/images/default/img-default.png" class="card-img-top rounded-0" alt="..."> #}
        <div class="card-img-overlay">
            <div class="card-title d-flex justify-content-between mx-3">
                <a href="{{ path('product_show', {'id': product.id}) }}" class="text-secondary mr-3"><i class="fas fa-eye"></i></a>
                <a href="{{ path('whishlist_add', {'id': product.id}) }}" class="card-link text-danger"><i class="far fa-heart"></i></a>
            </div>
        </div>
        <div class="mt-3 ml-1 d-flex justify-content-between mx-3">
            <p class="font-weight-bold">{{ product.name }}</p>
            <small>{{ product.price }} &euro;</small>
        </div>
    </div>
</div>