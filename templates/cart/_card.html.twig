<div class="col-4 mb-3">
    <div class="card border-0">
        <img src="/images/default/img-default.png" class="card-img-top rounded-0" alt="...">
        <div class="mt-3 ml-3">
            <h5 class="card-title font-italic">{{ item.product.name }}</h5>
            <p class="card-text font-weight-bold">Quantité: {{ item.quantity }} {{ item.product.price }} &euro;</p>
            <p class="card-text font-weight-bold">Total :{{ item.product.price * item.quantity }} &euro;</p>
            <a href="#" class="btn btn-danger btn-sm"><i class="fas fa-trash-alt"></i></a>
        </div>
    </div>
</div>

