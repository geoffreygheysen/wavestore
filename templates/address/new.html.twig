{% extends 'base.html.twig' %}

{% block title %}New Address{% endblock %}

{% block body %}
    <div class="container">

        <div class="row my-5">
            <h3>Adresses</h3>
        </div>

        <div class="row my-5">
            <div class="col-8">
                <div class="card rounded-0">
                    <div class="card-body">
                        {{ include('address/_form.html.twig') }}
                    </div>
                </div>
                <a href="{{ path('address_index') }}" class="btn btn-dark btn-lg my-5">retour</a>
            </div>

            {# menu #}
            <div class="col-4">
                {% include 'includes/menu-profil.html.twig' %}
            </div>

        </div>
    </div>

{% endblock %}
