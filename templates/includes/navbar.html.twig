<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Store</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Contact</a>
                </li>
            </ul>
            <!--btn inscription & connexion/deconnexion-->
            {% if not app.user %}                                    
            <form class="form-inline my-2 my-lg-0" action="{{ path('security_logout') }}">
                <button class="btn btn-outline-success my-2 my-sm-0 mx-2" type="submit">Se connecter</button>
            </form>
            <form class="form-inline my-2 my-lg-0 mr-2" action="{{ path('security_registration') }}">
                <button class="btn btn-outline-primary my-2 my-sm-0" type="submit">S'inscrire</button>
            </form>
            {% else %}            
            <form class="form-inline my-2 my-lg-0" action="{{ path('security_logout') }}">
                <button class="btn btn-outline-danger my-2 my-sm-0 mx-2" type="submit">Se déconnecter</button>
            </form>
            
            <a class="nav-link" href="#">Mon profil</a>

            {% endif %}
            
        </div>
    </nav>
</header>